---
name: Bug
about: Something crashes, or produces a wrong result
labels: bug
---

**Command** — the exact one, including flags:

```
```

**What happened** — traceback in full, or the wrong output:

```
```

**What you expected instead:**

**Environment:** output of `python -V`, your OS, and whether the venv came from
`make install` or `make demo`.

**Does `make demo` work?** It runs offline with no API key, so it separates
"the project is broken" from "my credentials or network are".
